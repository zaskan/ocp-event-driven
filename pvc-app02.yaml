kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: httpd-pvc
  namespace: app02
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: ocs-storagecluster-cephfs
  volumeMode: Filesystem
