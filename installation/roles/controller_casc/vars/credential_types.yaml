custom_credential_types:
  - name: "ServiceNow"
    kind: cloud
    inputs: "{{ lookup('file', 'files/snow-inputs.json') }}"
    injectors: "{{ lookup('file', 'files/snow-injectors.json') }}"
  - name: "Gitea"
    kind: cloud
    inputs: "{{ lookup('file', 'files/gitea-inputs.json') }}"
    injectors: "{{ lookup('file', 'files/gitea-injectors.json') }}"
  - name: "EDA"
    kind: cloud
    inputs: "{{ lookup('file', 'files/eda-inputs.json') }}"
    injectors: "{{ lookup('file', 'files/eda-injectors.json') }}"
  - name: "ArgoCD"
    kind: cloud
    inputs: "{{ lookup('file', 'files/argocd-inputs.json') }}"
    injectors: "{{ lookup('file', 'files/argocd-injectors.json') }}"
