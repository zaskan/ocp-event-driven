- name: Create Extra Objects in Openshift (Outside Argocd)
  kubernetes.core.k8s:
    state: present
    src: "{{ item }}"
  with_fileglob:
    - "files/alertmanager/*"
    - "files/app02/namespace/*"
    - "files/app02/others/*"